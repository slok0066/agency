[build]
  command = "npm run build"
publish = ".next"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22.16.0"

[[redirects]]
  from = "/api/*"
  to = "/api/:splat"
  status = 200

[dev]
  command = "npm run dev"
